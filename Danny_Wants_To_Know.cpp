#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int N, Q;
        cin >> N >> Q;
        int arr[N];
        int arrb[N];
        for (int i = 0; i < N; i++)
        {
            cin >> arr[N];
        }
        for (int i = 0; i < N; i++)
        {
            cin >> arrb[i];
        }
        // int total = 0;
        // while (Q--)
        // {
        //     int L, R;
        //     cin >> L >> R;
        //     int X;
        //     for (int i = L - 1; i < R; i++)
        //     {
        //         X = (arr[i] * arrb[i]);
        //         total = total + X;
        //     }
        // }
        // cout << total << endl;
    }
    return 0;
}